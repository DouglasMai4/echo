{
	"name": "api",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"auth:generate": "bunx @better-auth/cli generate --config src/lib/auth.lib.ts"
	},
	"dependencies": {
		"@better-auth/stripe": "^1.4.18",
		"@echo/database": "workspace: *",
		"better-auth": "^1.4.18",
		"hono": "^4.11.9",
		"stripe": "^20.3.1"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
